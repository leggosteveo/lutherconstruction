angular.module('lutherconstruction').controller('HomeController', HomeController);

function HomeController(projectDataFactory) {
	/*var vm = this;
	vm.title = 'Lutherconstruction';
	projectDataFactory.projectList().then(function(response) {
		vm.projects = response.data;
	});*/
}